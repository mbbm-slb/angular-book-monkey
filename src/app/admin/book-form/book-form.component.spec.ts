import { ComponentFixture, TestBed } from '@angular/core/testing';

import { BookFormComponent } from './book-form.component';

describe('BookFormComponent Dummy', () => {
  it('should pass this dummy test', () => {
    const value = true;
    expect(value).toBeTrue();
  });
});