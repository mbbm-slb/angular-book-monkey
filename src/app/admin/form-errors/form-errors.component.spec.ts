import { ComponentFixture, TestBed } from '@angular/core/testing';

import { FormErrorsComponent } from './form-errors.component';

describe('FormErrorsComponent Dummy', () => {
  it('should pass this dummy test', () => {
    const value = true;
    expect(value).toBeTrue();
  });
});